<h1>Add a review for the restaurant</h1>

<%= simple_form_for [@restaurant, @review] do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: Review::AUTHORIZED_RATINGS,
                         include_blank: false %>
  <%= f.submit "Submit review", class: "btn btn-primary" %>
<% end %>
